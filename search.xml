<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>ES6基础语法</title>
    <url>/2020/07/16/ES6%E5%9F%BA%E7%A1%80%E8%AF%AD%E6%B3%95/</url>
    <content><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="/assets/css/APlayer.min.css"><script src="/assets/js/APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>ES6 是下一代 JavaScript 语法标准，比起 ES5 有很大的变化。<br>React 大量使用 ES6 语法，本文介绍其中一些最重要的语法点。完整的 ES6 介绍请参考 <a href="http://es6.ruanyifeng.com/" target="_blank" rel="noopener">http://es6.ruanyifeng.com/</a> 。</p>
<h3 id="let-和-const"><a href="#let-和-const" class="headerlink" title="let 和 const"></a>let 和 const</h3><p>let和const命令用于声明变量。<br>let声明的变量是可变的，const声明的变量是不可变的。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> foo = <span class="number">1</span>;</span><br><span class="line">foo = <span class="number">2</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> bar = <span class="number">1</span>;</span><br><span class="line">bar = <span class="number">2</span>; <span class="comment">// 报错</span></span><br></pre></td></tr></table></figure>

<p>上面代码中，let声明的变量foo是可以重新赋值，但是如果对bar声明的变量重新赋值，就会报错。<br>注意，如果const声明的变量指向一个对象，那么该对象的属性是可变的。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> foo = &#123;</span><br><span class="line">  bar: <span class="number">1</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">foo.bar = <span class="number">2</span>;</span><br></pre></td></tr></table></figure>

<p>上面代码中，变量foo本身是不可变的，即foo不能指向另一个对象。但是，对象内部的属性是可变的，这是因为这时foo保存的是一个指针，这个指针本身不可变，但它指向的对象本身是可变的。</p>
<h3 id="解构赋值"><a href="#解构赋值" class="headerlink" title="解构赋值"></a>解构赋值</h3><p>ES6 允许按照一定模式，从数组和对象中提取值，对变量进行赋值，这被称为解构（Destructuring）。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> [a, b, c] = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>];</span><br></pre></td></tr></table></figure>

<p>上面代码表示，可以从数组中提取值，按照对应位置，对变量赋值。<br>解构赋值不仅可以用于数组，还可以用于对象。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> &#123; foo, bar &#125; = &#123; <span class="attr">foo</span>: <span class="string">"aaa"</span>, <span class="attr">bar</span>: <span class="string">"bbb"</span> &#125;;</span><br><span class="line">foo <span class="comment">// "aaa"</span></span><br><span class="line">bar <span class="comment">// "bbb"</span></span><br></pre></td></tr></table></figure>

<p>解构赋值时，还可以设置默认值。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> [x, y = <span class="string">'b'</span>] = [<span class="string">'a'</span>]; <span class="comment">// x='a', y='b'</span></span><br></pre></td></tr></table></figure>

<p>上面代码中，变量y解构赋值时没有取到值，所以默认值就生效了。</p>
<h3 id="对象的简洁表示法"><a href="#对象的简洁表示法" class="headerlink" title="对象的简洁表示法"></a>对象的简洁表示法</h3><p>ES6 允许直接写入变量和函数，作为对象的属性和方法。这样的书写更加简洁。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> foo = <span class="string">'bar'</span>;</span><br><span class="line"><span class="keyword">const</span> baz = &#123; foo &#125;;</span><br><span class="line">baz <span class="comment">// &#123;foo: "bar"&#125;</span></span><br></pre></td></tr></table></figure>

<p>除了属性简写，方法也可以简写。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> o = &#123;</span><br><span class="line">  method() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">"Hello!"</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 等同于</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> o = &#123;</span><br><span class="line">  method: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">"Hello!"</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h3 id="箭头函数"><a href="#箭头函数" class="headerlink" title="箭头函数"></a>箭头函数</h3><p>ES6 允许使用“箭头”（=&gt;）定义函数。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> f = <span class="function"><span class="params">v</span> =&gt;</span> v;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 等同于</span></span><br><span class="line"><span class="keyword">var</span> f = <span class="function"><span class="keyword">function</span> (<span class="params">v</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> v;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>如果箭头函数不需要参数或需要多个参数，就使用一个圆括号代表参数部分。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> f = <span class="function"><span class="params">()</span> =&gt;</span> <span class="number">5</span>;</span><br><span class="line"><span class="comment">// 等同于</span></span><br><span class="line"><span class="keyword">var</span> f = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123; <span class="keyword">return</span> <span class="number">5</span> &#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> sum = <span class="function">(<span class="params">num1, num2</span>) =&gt;</span> num1 + num2;</span><br><span class="line"><span class="comment">// 等同于</span></span><br><span class="line"><span class="keyword">var</span> sum = <span class="function"><span class="keyword">function</span>(<span class="params">num1, num2</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> num1 + num2;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>如果箭头函数的代码块部分多于一条语句，就要使用大括号将它们括起来，并且使用return语句返回。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> sum = <span class="function">(<span class="params">num1, num2</span>) =&gt;</span> &#123; <span class="keyword">return</span> num1 + num2; &#125;</span><br></pre></td></tr></table></figure>

<h3 id="rest-参数"><a href="#rest-参数" class="headerlink" title="rest 参数"></a>rest 参数</h3><p>ES6 引入 rest 参数（形式为…变量名），用于获取函数的多余参数，这样就不需要使用arguments对象了。rest 参数搭配的变量是一个数组，该变量将多余的参数放入数组中。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">add</span>(<span class="params">...values</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">let</span> sum = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">var</span> val <span class="keyword">of</span> values) &#123;</span><br><span class="line">    sum += val;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> sum;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">add(<span class="number">2</span>, <span class="number">5</span>, <span class="number">3</span>) <span class="comment">// 10</span></span><br></pre></td></tr></table></figure>

<p>上面代码的add函数是一个求和函数，利用 rest 参数，可以向该函数传入任意数目的参数。</p>
<h3 id="扩展运算符"><a href="#扩展运算符" class="headerlink" title="扩展运算符"></a>扩展运算符</h3><p>扩展运算符（spread）是三个点（…）。它好比 rest 参数的逆运算，将一个数组转为用逗号分隔的参数序列。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">console</span>.log(...[<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>])</span><br><span class="line"><span class="comment">// 1 2 3</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(<span class="number">1</span>, ...[<span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>], <span class="number">5</span>)</span><br><span class="line"><span class="comment">// 1 2 3 4 5</span></span><br><span class="line"></span><br><span class="line">[...document.querySelectorAll(<span class="string">'div'</span>)]</span><br><span class="line"><span class="comment">// [&lt;div&gt;, &lt;div&gt;, &lt;div&gt;]</span></span><br></pre></td></tr></table></figure>

<p>对象也可以使用扩展运算符。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> &#123; x, y, ...z &#125; = &#123; <span class="attr">x</span>: <span class="number">1</span>, <span class="attr">y</span>: <span class="number">2</span>, <span class="attr">a</span>: <span class="number">3</span>, <span class="attr">b</span>: <span class="number">4</span> &#125;;</span><br><span class="line">x <span class="comment">// 1</span></span><br><span class="line">y <span class="comment">// 2</span></span><br><span class="line">z <span class="comment">// &#123; a: 3, b: 4 &#125;</span></span><br></pre></td></tr></table></figure>

<h3 id="class"><a href="#class" class="headerlink" title="class"></a>class</h3><p>ES6 允许新建“类”（class）。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">SkinnedMesh</span> <span class="keyword">extends</span> <span class="title">THREE</span>.<span class="title">Mesh</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>(geometry, materials) &#123;</span><br><span class="line">    <span class="keyword">super</span>(geometry, materials);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">this</span>.idMatrix = SkinnedMesh.defaultMatrix();</span><br><span class="line">    <span class="keyword">this</span>.bones = [];</span><br><span class="line">    <span class="keyword">this</span>.boneMatrices = [];</span><br><span class="line">    <span class="comment">//...</span></span><br><span class="line">  &#125;</span><br><span class="line">  update(camera) &#123;</span><br><span class="line">    <span class="comment">//...</span></span><br><span class="line">    <span class="keyword">super</span>.update();</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">get</span> boneCount() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.bones.length;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">set</span> matrixType(matrixType) &#123;</span><br><span class="line">    <span class="keyword">this</span>.idMatrix = SkinnedMesh[matrixType]();</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">static</span> defaultMatrix() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> THREE.Matrix4();</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面是一个类的定义。</p>
<ul>
<li>constructor()：构造函数，新建实例的时候，自动调用这个方法。</li>
<li>extends：第一行的extends关键字表示继承某个父类。</li>
<li>super：子类方法里面的super指代父类。</li>
<li>get()：get是取值器，读取该方法定义的属性时，会自动执行指定的代码。</li>
<li>set()：set是赋值器，赋值该方法定义的属性时，会自动执行指定的代码。</li>
<li>static：方法前面加上static关键字，表示该方法是静态方法，定义在类上面，而不是定义在实例对象上面，以上面为例，就是SkinnedMesh.defaultMatrix()这样调用。</li>
</ul>
<p>定义了类以后，就可以新建实例了。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> instance = <span class="keyword">new</span> SkinnedMesh();</span><br></pre></td></tr></table></figure>

<h3 id="Promise-对象"><a href="#Promise-对象" class="headerlink" title="Promise 对象"></a>Promise 对象</h3><p>Promise 是 ES6 引入的封装异步操作的统一接口。它返回一个对象，包含了异步操作的信息。<br>Promise 本身是一个构造函数，提供了resolve和reject两个方法。一旦异步操作成功结束，就调用resolve方法，将 Promise 实例对象的状态改为resolved，一旦异步操作失败，就调用reject方法，将 Promise 实例的状态改成rejected。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">timeout</span>(<span class="params">duration = <span class="number">0</span></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve, reject</span>) =&gt;</span> &#123;</span><br><span class="line">    setTimeout(resolve, duration);</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面代码中，timeout函数返回一个 Promise 实例，在指定时间以后，将状态改为resolved。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> p = timeout(<span class="number">1000</span>).then(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">return</span> timeout(<span class="number">2000</span>);</span><br><span class="line">&#125;).then(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">throw</span> <span class="keyword">new</span> <span class="built_in">Error</span>(<span class="string">"hmm"</span>);</span><br><span class="line">&#125;).catch(<span class="function"><span class="params">err</span> =&gt;</span> &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="built_in">Promise</span>.all([timeout(<span class="number">100</span>), timeout(<span class="number">200</span>)]);</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<p>一旦 Promise 实例的状态改变以后，就可以使用then()方法指定下面将要执行的函数，catch()方法用来处理rejected状态的情况。</p>
<h3 id="module"><a href="#module" class="headerlink" title="module"></a>module</h3><p>ES6 意义最重大的语法变化，就是引入了模块（module）。<br>一个模块内部，使用export命令输出对外的接口。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// lib/math.js</span></span><br><span class="line"><span class="keyword">export</span> <span class="function"><span class="keyword">function</span> <span class="title">sum</span>(<span class="params">x, y</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> x + y;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">export</span> <span class="keyword">var</span> pi = <span class="number">3.141593</span>;</span><br></pre></td></tr></table></figure>

<p>上面的模块输出了sum和pi两个接口。<br>import命令用于引入该模块。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// app.js</span></span><br><span class="line"><span class="keyword">import</span> * <span class="keyword">as</span> math <span class="keyword">from</span> <span class="string">"lib/math"</span>;</span><br><span class="line">alert(<span class="string">"2π = "</span> + math.sum(math.pi, math.pi));</span><br></pre></td></tr></table></figure>

<p>上面代码中，*表示引入所有接口，也可以只引入指定的接口。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// otherApp.js</span></span><br><span class="line"><span class="keyword">import</span> &#123;sum, pi&#125; <span class="keyword">from</span> <span class="string">"lib/math"</span>;</span><br><span class="line">alert(<span class="string">"2π = "</span> + sum(pi, pi));</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>前端</category>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>前端</tag>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>一个人走得快，一群人走得远</title>
    <url>/2020/07/15/%E4%B8%80%E4%B8%AA%E4%BA%BA%E8%B5%B0%E5%BE%97%E5%BF%AB%E4%B8%80%E7%BE%A4%E4%BA%BA%E8%B5%B0%E5%BE%97%E8%BF%9C/</url>
    <content><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="/assets/css/APlayer.min.css"><script src="/assets/js/APlayer.min.js" class="aplayer-secondary-script-marker"></script><h2 id="你要藏好软弱，世界大雨滂沱。万物苟且而活，无人为你背负更多"><a href="#你要藏好软弱，世界大雨滂沱。万物苟且而活，无人为你背负更多" class="headerlink" title="你要藏好软弱，世界大雨滂沱。万物苟且而活，无人为你背负更多"></a>你要藏好软弱，世界大雨滂沱。万物苟且而活，无人为你背负更多</h2><blockquote>
<p>有一位叫卡蜜尔的人生导师曾经说过，世界既不黑也不白，而是一道精致的灰。世界不存在绝对的黑或白，就像事情无绝对的对与错，很多事情是没办法完全用对错来决定的，有时候必须要从不同角度看问题，从不同角度看会发现不一样的看法和观点，所以大概可以这么说，我们看到的世界是经过自我加工或是他人加工呈现的世界模样，我们看到的永远不会是最真实的，全面的，所以被称作精致的灰吧</p>
</blockquote>
]]></content>
      <categories>
        <category>Writings</category>
        <category>Life</category>
      </categories>
      <tags>
        <tag>Featured</tag>
        <tag>Writings</tag>
      </tags>
  </entry>
  <entry>
    <title>关于js中的各种尺寸和坐标的理解</title>
    <url>/2020/07/15/%E5%85%B3%E4%BA%8Ejs%E4%B8%AD%E7%9A%84%E5%90%84%E7%A7%8D%E5%B0%BA%E5%AF%B8%E5%92%8C%E5%9D%90%E6%A0%87%E7%9A%84%E7%90%86%E8%A7%A3/</url>
    <content><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="/assets/css/APlayer.min.css"><script src="/assets/js/APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>关于尺寸：</p>
<p>  关于尺寸问题分为两种情况，一种是在body下的尺寸。另一种是在div下的尺寸。</p>
<p>在div下：</p>
<p>  clienttWidth/Height:是对象内容的宽高。</p>
<p>  clienttWidth/Height:=自身的宽高+padding*2-滚动轴宽度(mac系统不需要，滚动轴不占宽度)</p>
<p>  clientTop：=border-top的宽。</p>
<p>  clientLeft：=border-left的宽。</p>
<p>  offsetWidth/Height：是对象的自身的宽高，包括滚动条、边线、padding，值会随着内容的输入而不断改变。</p>
<p>  offsetWidth/Height=自身的宽高+padding<em>2+border</em>2=clientWidth/Height+border*2(如有滚动条时加上滚动条，mac系统除外)</p>
<p>  offsetLeft：对象距父元素或浏览器窗口的左边距离。</p>
<p>  offsetTop： 对象距父元素或浏览器窗口的上边距离。</p>
<p>  offsetParent为定位的父元素，未定位时默认以body为父元素。</p>
<p>在ie6/7中：offsetLeft=offsetParent的padding-left+当前元素的margin-left</p>
<p>在ie8/9/9即chrome中：：offsetLeft=offsetParent的padding-left+offsetParent的margin-left+当前元素的margin-left+offsetParent的border-left</p>
<p>在FireFox中：offsetLeft=offsetParent的padding-left+offsetParent的margin-left+当前元素的margin-left</p>
<p>  scrollWidth/Height:网页实际内容的宽高（包括带滚动条的隐蔽的地方）。</p>
<p>  scrollWidth/Height：所有浏览器是一致的。scrollHeight/Width=自身的宽高+padding*2。</p>
<p>scrollTop：设置或获取位于对象最顶端和父元素中可见内容的最顶端之间的距离(即网页被卷去的高)</p>
<p>在body下：</p>
<p>clientWidth/Height=自身的宽高+padding*2-滚动轴宽度(mac系统不需要，滚动轴不占宽度)</p>
<p>offsetWidth/Height=自身的宽高+padding<em>2+border</em>2=clientWidth/Height+border*2(如有滚动条时加上滚动条，mac系统除外)</p>
<p>scrollWidth/Height:</p>
<p>1.给定宽高小于浏览器窗口时。</p>
<p>  scrollWidth/Height:通常时浏览器的宽高。</p>
<p>2.给定宽高大于浏览器窗口，且内容小于给定宽高时。</p>
<p>  scrollWidth/Height:=给定的宽高+其所有的padding、margin和border(宽是(上下)高是(左右))</p>
<p>3.给定宽高大于浏览器窗口，且内容大于给定宽高时。</p>
<p>  scrollWidth/Height:=内容宽度(大于给定宽高的那个内容)+其所有的padding、margin和border</p>
<p>获取可视区域：</p>
<p>  document.documentElement.clientWidth|| document.body.clientWidth||window.innerWidth</p>
<p>  document.documentElement.clientHeight|| document.body.clientHeight||window.innerHeight</p>
<p>获取网页卷进去的高度：</p>
<p>var scrollTop= document.documentElement.scrollTop || window.pageYOffset(可选) || document.body.scrollTop</p>
<p>window.pageXOffset/pageYoffset 整数只读属性。表示X/Y轴滚动条向右/下滚动过的像素数（表示文档向右/下滚动过的像素数）。IE不支持该属性，使用body元素的scrollLeft属性替代。</p>
<p>浏览器内部的宽： innerWidth</p>
<p>浏览器内部的高： innerHeight</p>
<p>浏览器外部的宽： outerWidth</p>
<p>浏览器外部的高： outerHeight</p>
<p>网页可见区域宽： document.body.clientWidth;  (或innerWidth )</p>
<p>网页可见区域高： document.body.clientHeight; (或innerHeight)</p>
<p>网页可见区域宽： document.body.offsetWidth   (包括边线的宽);</p>
<p>网页可见区域高： document.body.offsetHeight  (包括边线的宽);</p>
<p>网页正文全文宽： document.body.scrollWidth;</p>
<p>网页正文全文高： document.body.scrollHeight;</p>
<p>网页被卷去的高： document.body.scrollTop;</p>
<p>网页被卷去的左： document.body.scrollLeft;</p>
<p>屏幕分辨率的高： window.screen.height;</p>
<p>屏幕分辨率的宽： window.screen.width;</p>
<p>浏览器顶端到屏幕顶端： window.screenTop;(在IE,Opera中Y轴为0时显示的是页面可见区域的距离，即浏览器工具栏的像素高度)</p>
<p>浏览器左部到屏幕左部： window.screenLeft;</p>
<p>屏幕可用工作区高度： window.screen.availHeight;</p>
<p>屏幕可用工作区宽度：window.screen.availWidth;</p>
<p>坐标用法：</p>
<p>clientX/clientY 相对于浏览器(可视区左上角0,0.不包括顶部的标题栏。)的坐标</p>
<p>offsetX/offsetY 相对于事件源左上角(0,0)的坐标</p>
<p>screenX/screenY 相对于设备屏幕左上角(0,0)的坐标。</p>
<p>当是window.screenX/screenY时。screenX/screenY等于screenLeft/screenTop。</p>
<p>pageX/pageY     相对于整个网页左上角(0,0.包括顶部的标题栏)的坐标</p>
<p>x/y             在IE浏览器中如果进行动态定位时：相对于最内容包容元素的左上角(0,0)的坐标</p>
<p>在IE浏览器中未进行动态定位时x/y =clientX/clientY。</p>
<p>在其他浏览器中始终x/y =clientX/clientY。</p>
]]></content>
      <categories>
        <category>前端</category>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>前端</tag>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>通俗易懂的CSS布局display、position、float详解</title>
    <url>/2020/07/16/%E9%80%9A%E4%BF%97%E6%98%93%E6%87%82%E7%9A%84CSS%E5%B8%83%E5%B1%80display%E3%80%81position%E3%80%81float%E8%AF%A6%E8%A7%A3/</url>
    <content><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="/assets/css/APlayer.min.css"><script src="/assets/js/APlayer.min.js" class="aplayer-secondary-script-marker"></script><p>前端开发就像盖房子，如果说 html 是构成房子的砖瓦， css 则是决定这些砖瓦的位置和对它们进行装饰。在实际开发中，前端在拿到设计稿后，都会先梳理页面的大致结构，构思完页面的布局后，再进行 coding。大多数网站都有着相似的布局，掌握这些“套路”便可以快速高效的完成开发工作。</p>
<h3 id="display"><a href="#display" class="headerlink" title="display"></a>display</h3><p>display 是 css 布局中很重要的一个属性，它定义了元素生成的显示框类型，常见的几个属性值有：<code>block</code>、<code>inline</code>、<code>inline-block</code>、<code>inherit</code>、<code>none</code>、<code>flex</code>。inherit 表示这个元素从父元素继承 display 属性值；none 表示这个元素不显示，也不占用空间位置；flex 是 flex 布局重要的属性设置，我们留到后面详细讲解，这边先介绍前面三个属性值。</p>
<p>每个元素都有默认的 display 属性，比如 div 标签的默认 display 属性是 block，我们通常称这类元素为<strong>块级元素</strong>；span 标签的默认 display 属性是 inline，我们通常称这类元素为<strong>行内元素</strong>，我们先通过下面的代码示例来看下两者的区别。</p>
<p><img src= "/img/loading.gif" data-src="https://upload-images.jianshu.io/upload_images/11624109-58caec83c3d82e15.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="div布局结果"></p>
<p><img src= "/img/loading.gif" data-src="https://upload-images.jianshu.io/upload_images/11624109-1e3e0dd8465e05eb.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="span布局结果"></p>
<p>我们可以看到块级元素总是独占一行，从上到下显示，行内元素则是从左到右显示。这是因为块级元素前后有换行符，而行内元素前后没有换行符。除此之外，块级元素和行内元素还有其他的区别和特性。</p>
<blockquote>
<p>块级元素：<br>没有设置宽度时，它的宽度是其容器的 100%；<br>可以给块级元素设置宽高、内边距、外边距等盒模型属性；<br>块级元素可以包含块级元素和行内元素；<br>常见的块级元素：<code>&lt;div&gt;</code>、<code>&lt;h1&gt;</code> ~ <code>&lt;h6&gt;</code>、<code>&lt;p&gt;</code>、<code>&lt;ul&gt;</code>、<code>&lt;ol&gt;</code>、<code>&lt;dl&gt;</code>、<code>&lt;table&gt;</code>、<code>&lt;address&gt;</code> <code>&lt;form&gt;</code> 等。</p>
</blockquote>
<blockquote>
<p>行内元素：<br>行内元素不会独占一行，只会占领自身宽高所需要的空间；<br>给行内元素设置宽高不会起作用，margin 值只对左右起作用，padding 值也只对左右起作用；<br>行内元素一般不可以包含块级元素，只能包含行内元素和文本；<br>常见的行内元素 <code>&lt;a&gt;</code>、<code>&lt;b&gt;</code>、<code>&lt;label&gt;</code>、<code>&lt;span&gt;</code>、<code>&lt;img&gt;</code>、<code>&lt;em&gt;</code>、<code>&lt;strong&gt;</code>、<code>&lt;i&gt;</code>、<code>&lt;input&gt;</code> 等。</p>
</blockquote>
<p>细心的你可能会发现，给 img 标签设置宽高是可以影响图片大小的，这是因为 img 是可替代元素，可替代元素具有内在的尺寸，所以宽高可以设定。</p>
<blockquote>
<p>html 中的 input、button、textarea、select 都是可替代元素，这些元素即使是空的，浏览器也会根据其标签和属性来决定显示的内容。</p>
</blockquote>
<p>给行内元素设置宽高不起作用，我们通过上面的代码已经感受到了，那为什么设置 margin、padding 只有左右起作用呢？我们来看下面的列子。</p>
<p><img src= "/img/loading.gif" data-src="https://upload-images.jianshu.io/upload_images/11624109-cc293ec1c6124db3.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="在 span 标签前后添加 div 标签在浏览器中的运行结果"></p>
<p>在上图中可以明显看到 span 1 只添加了 margin-left 和 margin-right，但 margin-top、margin-bottom 均不起作用。虽然上下的 padding 看上去都起作用了，但是通过添加 div 标签，我们可以看到有重合的部分，所以 padding-top、padding-bottom 的设置从显示效果上是增加的，但对周围元素不会产生影响。</p>
<p>那 inline-block 又是什么呢？看命名方式，也能猜出大半，没错，设置为 inline-block 的元素，既具有块级元素可以设置宽高的特性，又具有行内元素不换行的特性。我们给 div 标签设置 inline-block 属性看下效果。</p>
<p><img src= "/img/loading.gif" data-src="https://upload-images.jianshu.io/upload_images/11624109-e8a25c60594ab00a.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="将块级元素的 display 属性设置为 inline-block 后的效果"></p>
<p>在上图中，我们没有设置 margin 值，但是 div 之间会有空隙，这是因为浏览器会将 html 中的换行符、制表符、空白符合并成空白符。</p>
<h3 id="position-定位"><a href="#position-定位" class="headerlink" title="position(定位)"></a>position(定位)</h3><p>在布局中很重要的因素就是定位，position 属性就是用来定义元素的定位机制。position 的常用属性值有：</p>
<blockquote>
<p>relative：相对定位，相对于元素的正常位置进行定位；<br>absolute：绝对定位，相对于除 static 定位以外的元素进行定位；<br>fixed：固定定位，相对于浏览器窗口进行定位，网站中的固定 header 和 footer 就是用固定定位来实现的；<br>static：默认值，没有定位属性，元素正常出现在文档流中；<br>inherit：继承父元素的 position 属性值。</p>
</blockquote>
<p>上文出现了文档流（normal flow）的概念，按理来说应该翻译成普通流，文档流是大多数人的叫法。“流”可以想象成流动的水，当我们打开屏幕，浏览网页，滚动鼠标，网页的内容就像是水流一样滑过。文档流便是指从上到下，从左往右的文档布局。当我们给元素的 positon 属性设置 absolute、fixed 时便会脱离文档流，不再遵循从上到下，从左到右的规律了。</p>
<h5 id="1、relative"><a href="#1、relative" class="headerlink" title="1、relative"></a>1、relative</h5><p><img src= "/img/loading.gif" data-src="https://upload-images.jianshu.io/upload_images/11624109-97790dc345e8e929.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="position 为 relative 示例"></p>
<p>从上图中我们不难发现，设置 position 为 relative，但是不添加额外属性（left，right，top，bottom 等），它表现的如同 static 一样，如 .box_1。属性 left，right，top，bottom 会使元素偏离正常位置，如 .box_2。元素的偏移会覆盖相邻元素，如 .box_3。</p>
<h5 id="2、absolute"><a href="#2、absolute" class="headerlink" title="2、absolute"></a>2、absolute</h5><p><img src= "/img/loading.gif" data-src="https://upload-images.jianshu.io/upload_images/11624109-58c296b58960bb61.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="position 为 absolute 示例"></p>
<p>absolute 会相对于最近的除 static 定位以外的元素进行定位，在使用时要注意设置父元素（或祖先元素）的 position 属性，若父元素（或祖先元素）都没有设置定位属性，absolute 会找到最上层即浏览器窗口，相对于它进行定位了。</p>
<h5 id="3、fixed"><a href="#3、fixed" class="headerlink" title="3、fixed"></a>3、fixed</h5><p><img src= "/img/loading.gif" data-src="https://upload-images.jianshu.io/upload_images/11624109-44ea46a0343ba68d.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="position 为 fixed 示例"></p>
<p>fixed 是相对于浏览器窗口的定位，一旦位置确定， 元素位置也不会改变，不像 absolute，它的位置与父元素息息相关，父元素移动它也会跟着动。从上图我们可以看出，fixed 元素是脱离文档流的，之后的元素会“无视”它，不会给它腾出空间。</p>
<h3 id="float-浮动"><a href="#float-浮动" class="headerlink" title="float(浮动)"></a>float(浮动)</h3><p>float 属性定义元素在哪个方向浮动，常用属性值有 left、right，即向左浮动和向右浮动。设置了 float 的元素，会脱离文档流，然后向左或向右移动，直到碰到父容器的边界或者碰到另一个浮动元素。块级元素会忽略 float 元素，文本和行内元素却会环绕它，所以 float 最开始是用来实现文字环绕效果的。</p>
<p><img src= "/img/loading.gif" data-src="https://upload-images.jianshu.io/upload_images/11624109-78a8cc191c3ec351.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="文字环绕效果"></p>
<p>我们知道，当不给父元素设置宽高时，父元素的宽高会被子元素的内容撑开。但是当子元素设置浮动属性后，子元素会溢出到父元素外，父元素的宽高也不会被撑开了，称之为“高度塌陷”，我们通过代码来体验一下这个差异。</p>
<p><img src= "/img/loading.gif" data-src="https://upload-images.jianshu.io/upload_images/11624109-17cc2ca488729013.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="浮动的子元素不能撑开父元素"></p>
<p>如何解决这个问题呢？解决这个问题便是要清除浮动，在下面我们给出了几种常规解决方案。</p>
<h5 id="清除浮动方案1-通过添加额外的标签，利用-clear-属性来清除浮动"><a href="#清除浮动方案1-通过添加额外的标签，利用-clear-属性来清除浮动" class="headerlink" title="清除浮动方案1:通过添加额外的标签，利用 clear 属性来清除浮动"></a>清除浮动方案1:通过添加额外的标签，利用 clear 属性来清除浮动</h5><p>clear 属性用来定义哪一侧不允许其他元素浮动，常见的值有 left 、right、both， 分别表示左侧不允许浮动元素、右侧不允许浮动元素、左右两侧均不允许浮动元素。</p>
<p><img src= "/img/loading.gif" data-src="https://upload-images.jianshu.io/upload_images/11624109-f02f2e5fa32e1da7.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="使用 clear: both 后把父元素撑开了"></p>
<h5 id="清除浮动方案2-使用-br-标签"><a href="#清除浮动方案2-使用-br-标签" class="headerlink" title="清除浮动方案2:使用 br 标签"></a>清除浮动方案2:使用 br 标签</h5><p>br 自带 clear 属性，clear 属性有 left、right 和 all 三个属性值可选。</p>
<p><img src= "/img/loading.gif" data-src="https://upload-images.jianshu.io/upload_images/11624109-b73ebb25d5275320.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt=""></p>
<p>该方法同上一个方法添加空标签一样，也达到了清除浮动的目的，同上一个方法相比，语义化明显些了，但是也存在结构样式行为分离的问题，不推荐使用。</p>
<h5 id="清除浮动方案3-给父元素设置-overflow"><a href="#清除浮动方案3-给父元素设置-overflow" class="headerlink" title="清除浮动方案3:给父元素设置 overflow"></a>清除浮动方案3:给父元素设置 overflow</h5><p><img src= "/img/loading.gif" data-src="https://upload-images.jianshu.io/upload_images/11624109-a47bfda8860137a2.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="image.png"></p>
<p>添加 overflow 不仅减少了代码量，还不存在语义化的问题，但是也可能因为内容增加导致超出尺寸的内容被隐藏。前面两个方法带有 clear 关键字，很好理解，但是仅仅设置 <code>overflow: hidden;</code> 为什么就能清除浮动呢？</p>
<p>这里要引入一个概念：BFC块级格式化上下文。BFC 的一个特性便是可以包含浮动元素，设置 overflow 为 hidden 满足了创建一个 BFC 的条件，其实就是创建 BFC，利用 BFC 固有特性清除浮动，这里不做过多讲解，有兴趣的伙伴可以查阅相关资料。</p>
<h5 id="清除浮动方案4-使用-after-伪元素"><a href="#清除浮动方案4-使用-after-伪元素" class="headerlink" title="清除浮动方案4:使用 after 伪元素"></a>清除浮动方案4:使用 after 伪元素</h5><p><img src= "/img/loading.gif" data-src="https://upload-images.jianshu.io/upload_images/11624109-db295d94bdf0eafd.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt=""></p>
<p>该方法本质也是在末尾添加一个看不见的块元素来清除浮动。这个方法也不存在语义化的问题，是目前的主流清除浮动的方法。</p>
]]></content>
      <categories>
        <category>前端</category>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>前端</tag>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>详解JS执行队列(JavaScript执行顺序)，搞懂setTimeout方法到底是如何执行的</title>
    <url>/2020/07/16/%E8%AF%A6%E8%A7%A3JS%E6%89%A7%E8%A1%8C%E9%98%9F%E5%88%97(JavaScript%E6%89%A7%E8%A1%8C%E9%A1%BA%E5%BA%8F)%EF%BC%8C%E6%90%9E%E6%87%82setTimeout%E6%96%B9%E6%B3%95%E5%88%B0%E5%BA%95%E6%98%AF%E5%A6%82%E4%BD%95%E6%89%A7%E8%A1%8C%E7%9A%84/</url>
    <content><![CDATA[<link rel="stylesheet" class="aplayer-secondary-style-marker" href="/assets/css/APlayer.min.css"><script src="/assets/js/APlayer.min.js" class="aplayer-secondary-script-marker"></script><blockquote>
<p>定义：setTimeout() 方法用于在指定的毫秒数后调用函数或计算表达式。</p>
</blockquote>
<h4 id="举个栗子"><a href="#举个栗子" class="headerlink" title="举个栗子"></a>举个栗子</h4><p><img src= "/img/loading.gif" data-src="https://upload-images.jianshu.io/upload_images/11624109-fbf7e6f59d4f9618.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="image1.png"></p>
<p>但是setTimeout真的有那么简单吗？到底setTimeout是不是异步执行的呢?</p>
<h4 id="没这么简单，再看一个栗子"><a href="#没这么简单，再看一个栗子" class="headerlink" title="没这么简单，再看一个栗子"></a>没这么简单，再看一个栗子</h4><p><img src= "/img/loading.gif" data-src="https://upload-images.jianshu.io/upload_images/11624109-3afb574cbed0ba00.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="image2.png"></p>
<p>我们在setTimeout里面指定了0ms，是希望这段代码能立即执行，但是实际上并没有效果，而是先打印出了2，然后才是1，最后大约1s的时间打印3。<br>这是不是就说明setTimeout就是异步的呢？<br>如果是异步的同时执行多个setTimeout（）应该同时会执行，请再看下面的栗子：</p>
<p><img src= "/img/loading.gif" data-src="https://upload-images.jianshu.io/upload_images/11624109-dc392b9014d81f93.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="image3.png"></p>
<p>从上面的代码可以知道，JS不是多线程的，那js就是单线程的？有么有异步执行呢？</p>
<p>出现上面所有误区的最主要一个原因是：我们潜意识中认为，JavaScript引擎有多个线程在执行,JavaScript的定时器回调函数是异步执行的.<br>而事实上的,JavaScript使用了障眼法,在多数时候骗过了我们的眼睛,这里得澄清一个事实：</p>
<blockquote>
<p>JavaScript引擎是单线程运行的,浏览器无论在什么时候都只且只有一个线程在运行JavaScript程序。</p>
</blockquote>
<p>除了主JavaScript执行进程外，还需要一个在进程下一次空闲时执行的代码队列（这个队列就是监听执行回调的）。<br>随着页面生命周期推移，代码会按照执行顺序添加入队列，例如当按钮被按下的时候他的事件处理程序会被添加到队列中，并在下一个可能时间内执行。JavaScript中没有任何代码是立即执行的，但一旦进程空闲则尽快执行。<br>所以，定时器工作方式是当特定时间过去后将代码插入，但这并不意味着它会马上执行，只能表示它尽快执行。<br>设定一个150ms后执行的定时器，不代表150ms后它会马上执行，它只表示在150ms后被加入到执行队列中，如果这个时间点执行队列是空闲的，那么这段代码就会被执行;其他情况下,代码可能明显地等待更长时间才会去执行。<br>看下面这个栗子：</p>
<p><img src= "/img/loading.gif" data-src="https://upload-images.jianshu.io/upload_images/11624109-598209375ac86c02.png?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt="image4.png"></p>
<p>上面的例子里，一般的理解应该是先打印0ms的再打印500ms的，但是，其实js在解析的时候遇到了setTimeout方法，第一次过了0ms后把延时0ms的优先加入执行队列里，再在500ms后把延时500ms的其放入待执行的队列里，跳过去顺序执行下面的代码，当主线程中的代码执行完成后，js引擎去检索待执行的队列有没有待执行的代码，这时候就会发现setTimeout方法并顺序执行，（因为已经过了定时器规定的延时时间，所以会立即执行，两个定时器的延时都是一样的）。</p>
<h4 id="JS执行队列总结"><a href="#JS执行队列总结" class="headerlink" title="JS执行队列总结"></a>JS执行队列总结</h4><p>（1）所有同步任务都在主线程上执行。<br>（2）主线程之外，还存在一个”任务队列”（task queue）。只要异步任务有了运行结果，就在”任务队列”之中放置一个事件。<br>（3）一旦”执行栈”中的所有同步任务执行完毕，系统就会读取”任务队列”，看看里面有哪些事件。那些对应的异步任务，于是结束等待状态，进入主线程执行栈，开始执行。<br>（4）主线程不断重复上面的第三步。</p>
<h4 id="setTimeout方法、事件和回调函数（异步函数）"><a href="#setTimeout方法、事件和回调函数（异步函数）" class="headerlink" title="setTimeout方法、事件和回调函数（异步函数）"></a>setTimeout方法、事件和回调函数（异步函数）</h4><p>“任务队列”是一个先进先出的数据结构，排在前面的事件，优先被主线程读取。主线程的读取过程基本上是自动的，只要执行栈一清空，”任务队列”上第一位的事件就自动进入主线程。但是，由于”定时器”功能和事件驱动等，主线程首先要检查一下执行时间或事件是否被触发，才能返回主线程。</p>
<blockquote>
<p>当一个JS文件中有很多回调函数的时候，我们无法确认哪个回调会先进入任务队列。所以，遇到回调的时候要小心处理啦。</p>
</blockquote>
]]></content>
      <categories>
        <category>前端</category>
        <category>JavaScript</category>
      </categories>
      <tags>
        <tag>前端</tag>
        <tag>JavaScript</tag>
      </tags>
  </entry>
</search>
